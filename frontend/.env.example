# Base URL of the BeviGrow API.
#
# Leave this EMPTY for local development — Vite proxies /api to
# http://127.0.0.1:8000 (see vite.config.ts), so same-origin requests work.
#
# On Render, set it to the deployed backend URL:
VITE_API_URL=https://bevigrow-backend.onrender.com
